#include <iostream>
#include <vector>
#include <cstdlib>

// int M = rand() % 10;
int N = rand() % 10;
int rNum;
std::vector<int> List;
for (int i = 0; i < N; i++)
{
    int M = rand() % 10;
    rNum = rand() % 10;
}
